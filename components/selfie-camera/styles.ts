import { StyleSheet } from "react-native";

const CAMERA_SIZE = 90;

const styles = StyleSheet.create({
  camera: {
    height: CAMERA_SIZE,
    width: CAMERA_SIZE,
    borderRadius: 50,
    backgroundColor: "lightgray",
  },
});

export default styles;
