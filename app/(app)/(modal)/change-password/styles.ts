import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    rowGap: 20,
    padding: 20,
  },
  headerText: {
    fontSize: 21,
    fontWeight: "500",
    textAlign: "center",
  },
});

export default styles;
