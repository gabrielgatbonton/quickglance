import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    padding: 16,
    paddingTop: 14,
  },
  contentContainer: {
    rowGap: 10,
    marginBottom: 30,
  },
  columnWrapper: {
    columnGap: 10,
  },
  headerContainer: {
    marginBottom: 10,
  },
  headerTitle: {
    fontSize: 18,
  },
});

export default styles;
