import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  contentContainer: {
    rowGap: 10,
  },
  segmentedControl: {
    marginTop: 3,
    marginBottom: 18,
  },
});

export default styles;
