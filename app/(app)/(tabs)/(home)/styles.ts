import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  columnWrapper: {
    columnGap: 10,
  },
  contentContainer: {
    rowGap: 10,
  },
  pageControlView: {
    height: 15,
  },
});

export default styles;
